# Copy to .devcontainer/.env — compose reads .env from the compose file's own
# directory, NOT the repo root.

# ABSOLUTE path to the shared Claude knowledge base. Compose does not expand ~.
CLAUDE_KB=/Users/ramuk/claude-kb

# Development database only; never a production credential.
POSTGRES_PASSWORD=homemgmt_dev
